import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.mojy.ashesmoonfortress",
  appName: "Ashes of the Moon Fortress",
  webDir: "dist",
};

export default config;
